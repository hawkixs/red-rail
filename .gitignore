__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
build/
dist/
*.egg-info/
.env

# Agent tool worktrees (local, per session)
.claude/worktrees/

# Drift snapshots are written to the ReD root (make audit); never here, the repository is public
docs/audits/
